# If using Docker, add ffmpeg in your Dockerfile (not here). Example:
# RUN apt-get update && apt-get install -y ffmpeg
requests
PyPDF2
fastapi
uvicorn
openai
python-multipart
pydantic
azure-cognitiveservices-speech
